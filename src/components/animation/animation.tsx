import * as React from 'react';
import { LoaderBlock, IconBlock } from './animation.style';

const Animation: React.FC = (): JSX.Element => (
  <LoaderBlock>
    <IconBlock />
  </LoaderBlock>
);

export default Animation;
